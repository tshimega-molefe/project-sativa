export default function calculateTransactionFee() {
  // TODO: Add conditions to check if a user has an active subscription or not to determine their transaction fee

  const baseCost = 2; // Sativa base transaction fee

  const transactionFee = baseCost;

  return transactionFee;
}
